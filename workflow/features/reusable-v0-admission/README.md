# Reusable v0 admission

This feature replaces the documentation-only placeholder with the first honest
durable implementation. `workflow.toml` is the state authority. The rejected
candidate was repaired and its implementation gates pass; admission remains
active until two fresh orchestrator-owned reviews pass on the final candidate.
