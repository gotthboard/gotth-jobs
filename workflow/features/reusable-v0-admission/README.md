# Reusable v0 admission

This feature replaces the documentation-only placeholder with the first honest
durable implementation. `workflow.toml` is the state authority. Evidence and
cold reviews remain incomplete until the implementation and all gates pass.
